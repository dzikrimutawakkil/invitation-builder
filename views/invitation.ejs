<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= title %></title>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Lato:wght@400;700&display=swap" rel="stylesheet">
<style>
    /* Restore the same styles from the builder so it looks identical */
    body { margin: 0; padding: 0; background: #333; font-family: 'Lato', sans-serif; min-height: 100vh; display: flex; justify-content: center; }
    
    .mobile-container {
        width: 100%; max-width: 400px; background: white; min-height: 100vh;
        box-shadow: 0 0 20px rgba(0,0,0,0.5);
    }

    /* Helper classes used in the builder */
    .hero-section { text-align: center; padding: 40px 20px; background: #f9f9f9; }
    .hero-title { font-family: 'Cinzel', serif; color: #2c3e50; font-size: 2em; margin: 10px 0; }
    .guest-badge { background: #f1c40f; color: #2c3e50; padding: 5px 10px; border-radius: 4px; font-weight: bold; }
    p { line-height: 1.6; color: #555; }
</style>
</head>
<body>

    <div class="mobile-container">
        <%- content %>
    </div>

</body>
</html>