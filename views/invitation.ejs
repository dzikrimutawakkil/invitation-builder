<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <link rel="stylesheet" href="/css/theme.css">
    <style>
        /* Basic Layout Only - Everything else is in theme.css */
        body { background: #333; display: flex; justify-content: center; min-height: 100vh; margin: 0; padding: 20px 0; }
        .mobile-container { width: 100%; max-width: 400px; background: white; min-height: 100vh; box-shadow: 0 0 20px rgba(0,0,0,0.5); display: flex; flex-direction: column; }
        .design-content { flex: 1; }
    </style>
</head>
<body>
    <div class="mobile-container" data-theme="<%= locals.theme || '' %>">
        <div class="design-content">
            <%- content %>
        </div>
    </div>
</body>
</html>