<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <link rel="stylesheet" href="/css/theme.css">
    <style>
        /* Small override just for the mobile viewer presentation */
        body { background: #333; display: flex; justify-content: center; min-height: 100vh; }
        .mobile-container { width: 100%; max-width: 400px; background: white; min-height: 100vh; box-shadow: 0 0 20px rgba(0,0,0,0.5); }
    </style>
</head>
<body>
    <div class="mobile-container">
        <%- content %>
    </div>
</body>
</html>